
fn main() {
    double[] points = new double[10];

    for (int i = 0; i <= 10; i++) points[i] = (i * 5);

    new LGraph (
        points,         // Points
        500,            // Preferred width
        500,            // Preffered height
    ).display();
}