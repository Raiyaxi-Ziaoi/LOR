#def String[] keywords = {"HELLOWORLD"};
#def String[] program = {"HELLOWORLD"};
#def MacroFunction[] functions = new MacroFunction[1];

fn main() {
    functions[0] = (tokens, stack, registers) l> {
        println!("Hello world!");
    };

    var macro = new Macro(keywords, functions);
    macro.parse(program);
}